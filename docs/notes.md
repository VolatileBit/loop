# Working notes

Scratch notes kept while the pipeline was taking shape. Superseded by the
README once the stage model settled.

- verify runs *after* implement, always, even when the session says it ran it
- worktree per issue is the eventual goal; rolling worktree first
- review verdicts need a stable machine-readable marker
