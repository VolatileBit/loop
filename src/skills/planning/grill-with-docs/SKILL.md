---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go. Run this only when explicitly asked for by name — it is a deliberate, user-initiated workflow, not one to start on your own.
---

Run a `/grilling` session, using the `/domain-modeling` skill.

Capture settled testing boundaries and unresolved human decisions so `to-spec` can preserve them. When asked to publish a spec or implementation issues, use `to-spec` or `to-issues` and their Loop planning contract; this interview alone does not create runnable issues.
